# Resend API Configuration (for contact form emails)
RESEND_API_KEY=re_your_api_key_here

# Contact Email - Where form submissions will be sent
CONTACT_EMAIL=sherpaangdawa092@gmail.com

# Airtable API Configuration (for comments system)
# Get your API key from: https://airtable.com/create/tokens
# Create a base with a table named "Comments" with these fields:
# - trek_slug (Single line text)
# - name (Single line text)
# - email (Email)
# - comment (Long text)
# - status (Single select: pending, approved, spam)
# - ip_hash (Single line text)
# - created_at (Date with time)
AIRTABLE_API_KEY=your_airtable_api_key_here
AIRTABLE_BASE_ID=your_airtable_base_id_here

# Unsplash API Configuration (for downloading images)
UNSPLASH_ACCESS_KEY=your_unsplash_access_key_here

# Node Environment
NODE_ENV=production